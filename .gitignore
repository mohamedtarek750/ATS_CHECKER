.env
__pycache__/
*.pyc
data/inbox/*
data/output/*
!data/inbox/.gitkeep
!data/output/.gitkeep
.venv/

# Never commit real applicants CVs
tests/fixtures/real_student_cv.pdf

# Streamlit secrets are per-deployment, never committed
.streamlit/secrets.toml
data/jobs/
data/output/candidates.db

# web build output
.vercel

# Next.js app (now at the repo root)
node_modules/
.next/
next-env.d.ts

# audit scratch cases
audit/_cases/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Applications sent through the public link: real people's CVs, names, emails
# and phone numbers. This directory must never reach the repository.
data/hiring/

# Google service account key for the Sheets/Drive backend.
service-account*.json
