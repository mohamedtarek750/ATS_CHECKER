# Runtime for the Vercel Python function and the local CLIs.
pypdf>=5
python-docx>=1.1
pydantic>=2
fastapi>=0.110
python-multipart>=0.0.9

# Optional: only needed for the provider you actually use.
google-genai>=1.0        # ATS_PROVIDER=gemini
# anthropic>=1,<2        # ATS_PROVIDER=claude

# Local tooling only - not needed on Vercel.
python-dotenv>=1

# Optional: only for ATS_BACKEND=sheets, which keeps applications in a Google
# Sheet and the CVs in Drive. The default backend is local files and needs none
# of this.
# google-api-python-client>=2
# google-auth>=2
