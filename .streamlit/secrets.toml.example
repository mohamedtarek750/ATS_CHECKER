# Streamlit Cloud: paste this into  Manage app -> Settings -> Secrets
# (locally you can keep using .env instead - this file is only for the cloud)

GEMINI_API_KEY = "AIza..."

# Optional: gate the public URL. Without this, anyone with the link can upload
# CVs and spend your API quota.
APP_PASSWORD = "pick-something-long"

# Optional
# ATS_PROVIDER = "gemini"
